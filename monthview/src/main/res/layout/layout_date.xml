<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

  <data>

    <variable
      name="vmdate"
      type="com.eddystudio.monthview.DateViewModel"/>
  </data>

  <RelativeLayout
    android:layout_width="68dp"
    android:layout_height="68dp"
    android:orientation="vertical">

    <TextView
      android:layout_width="48dp"
      android:layout_height="48dp"
      android:layout_gravity="center"
      android:layout_centerInParent="true"
      android:background="@{ vmdate.isToday ? @drawable/bg_today : null}"
      android:gravity="center"
      android:text="@{vmdate.dateObservable}"
      android:textAlignment="center"
      android:textColor="@{vmdate.isCurrentMonth ? @android:color/white : @android:color/darker_gray}"/>

  </RelativeLayout>
</layout>